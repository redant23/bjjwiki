# 내 기술(My Skills) 분류 저장 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 기술을 흥미생김/연습중/즐겨씀/나를 대표하는 기술(최대 2개) 중 하나로 분류하고, 별개로 좋아요를 표시할 수 있게 한다.

**Architecture:** `User.mySkills`를 `ObjectId[]`에서 `{technique, status, isFavorite}[]` 서브도큐먼트 배열로 바꾸고, 이를 다루는 API 3개(목록 조회/단건 조회/upsert)를 추가한다. 기술 상세 페이지에는 분류 컨트롤 컴포넌트를, 프로필 페이지에는 분류별 목록 섹션을 추가한다.

**Tech Stack:** Next.js 16 App Router, Mongoose 9, NextAuth v4.

**테스트 방법에 대한 메모:** 테스트 프레임워크가 없다. 각 태스크는 개발 서버를 띄운 상태에서 `curl`로 API를 검증하고, UI 태스크는 코드 리딩 + `tsc --noEmit`으로 검증한다(브라우저는 컨트롤러가 별도로 최종 확인한다).

---

## Task 1: User 모델 — `mySkills` 구조 변경

**Files:**
- Modify: `src/models/User.ts`

- [ ] **Step 1: interface와 스키마 교체**

`export interface IUserCombo {` 앞에 추가:
```typescript
export interface IUserSkill {
  technique: mongoose.Types.ObjectId;
  status: 'interested' | 'practicing' | 'frequently_used' | 'signature' | null;
  isFavorite: boolean;
}

```

`IUser` interface 안의 다음 줄:
```typescript
  mySkills: mongoose.Types.ObjectId[];
```
을 다음으로 교체:
```typescript
  mySkills: IUserSkill[];
```

`const ComboSchema = new Schema<IUserCombo>(` 바로 앞에 추가:
```typescript
const UserSkillSchema = new Schema<IUserSkill>(
  {
    technique: { type: Schema.Types.ObjectId, ref: 'Technique', required: true },
    status: {
      type: String,
      enum: ['interested', 'practicing', 'frequently_used', 'signature', null],
      default: null,
    },
    isFavorite: { type: Boolean, default: false },
  },
  { _id: false }
);

```

`UserSchema` 안의 다음 줄:
```typescript
    mySkills: [{ type: Schema.Types.ObjectId, ref: 'Technique' }],
```
을 다음으로 교체:
```typescript
    mySkills: { type: [UserSkillSchema], default: [] },
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: Commit**

```bash
git add src/models/User.ts
git commit -m "$(cat <<'EOF'
Restructure User.mySkills into status/favorite entries

Replaces the flat ObjectId[] with {technique, status, isFavorite}
subdocuments so a technique can be tagged with one of four mutually
exclusive stages plus an independent favorite flag. No migration
needed — mySkills has no real data yet.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: `GET /api/user/me/skills` (목록, populate)

**Files:**
- Create: `src/app/api/user/me/skills/route.ts`

- [ ] **Step 1: 라우트 작성**

```typescript
import { NextResponse } from 'next/server';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import dbConnect from '@/lib/db';
import User from '@/models/User';

export async function GET() {
  try {
    const session = await getServerSession(authOptions);

    if (!session) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized' },
        { status: 401 }
      );
    }

    await dbConnect();

    const user = await User.findById(session.user.id)
      .select('mySkills')
      .populate('mySkills.technique', 'name slug pathSlugs');

    if (!user) {
      return NextResponse.json(
        { success: false, error: 'User not found' },
        { status: 404 }
      );
    }

    return NextResponse.json({ success: true, data: user.mySkills });
  } catch (error) {
    console.error('GET /api/user/me/skills error:', error);
    return NextResponse.json(
      { success: false, error: 'Failed to fetch skills' },
      { status: 500 }
    );
  }
}
```

- [ ] **Step 2: 비로그인 401 확인**

```bash
curl -i http://localhost:3000/api/user/me/skills
```
Expected: `401`.

- [ ] **Step 3: 로그인 상태에서 빈 배열 확인**

```bash
COOKIE_JAR=/tmp/task_skills_list.txt
rm -f "$COOKIE_JAR"
CSRF_JSON=$(curl -s -c "$COOKIE_JAR" http://localhost:3000/api/auth/csrf)
CSRF=$(node -e "console.log(JSON.parse(process.argv[1]).csrfToken)" "$CSRF_JSON")
curl -s -b "$COOKIE_JAR" -c "$COOKIE_JAR" -X POST http://localhost:3000/api/auth/callback/credentials \
  -H "Content-Type: application/x-www-form-urlencoded" \
  --data-urlencode "csrfToken=$CSRF" \
  --data-urlencode "email=tester@example.com" \
  --data-urlencode "password=testpass123" \
  --data-urlencode "json=true"

curl -s -b "$COOKIE_JAR" http://localhost:3000/api/user/me/skills
```
Expected: `{"success":true,"data":[]}` (아직 아무것도 추가하지 않았으므로 — 만약 `tester@example.com`이 이전 세션에서 이미 뭔가 추가한 상태라면 빈 배열이 아닐 수 있는데, 그 경우 배열 형태와 각 항목이 `technique.name`/`technique.slug`를 포함하는지만 확인하면 된다).

- [ ] **Step 4: Commit**

```bash
git add src/app/api/user/me/skills/route.ts
git commit -m "$(cat <<'EOF'
Add GET /api/user/me/skills for the current user's tagged techniques

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `GET`/`PUT /api/user/me/skills/[techniqueId]`

**Files:**
- Create: `src/app/api/user/me/skills/[techniqueId]/route.ts`

- [ ] **Step 1: 라우트 작성**

```typescript
import { NextResponse } from 'next/server';
import mongoose from 'mongoose';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import dbConnect from '@/lib/db';
import User from '@/models/User';

const VALID_STATUSES = ['interested', 'practicing', 'frequently_used', 'signature'];

export async function GET(
  request: Request,
  props: { params: Promise<{ techniqueId: string }> }
) {
  const params = await props.params;
  try {
    const session = await getServerSession(authOptions);

    if (!session) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized' },
        { status: 401 }
      );
    }

    if (!mongoose.isValidObjectId(params.techniqueId)) {
      return NextResponse.json(
        { success: false, error: 'Invalid technique id' },
        { status: 400 }
      );
    }

    await dbConnect();

    const user = await User.findById(session.user.id).select('mySkills');

    if (!user) {
      return NextResponse.json(
        { success: false, error: 'User not found' },
        { status: 404 }
      );
    }

    const entry = user.mySkills.find(
      (s) => s.technique.toString() === params.techniqueId
    );

    return NextResponse.json({
      success: true,
      data: {
        status: entry?.status ?? null,
        isFavorite: entry?.isFavorite ?? false,
      },
    });
  } catch (error) {
    console.error('GET /api/user/me/skills/[techniqueId] error:', error);
    return NextResponse.json(
      { success: false, error: 'Failed to fetch skill' },
      { status: 500 }
    );
  }
}

export async function PUT(
  request: Request,
  props: { params: Promise<{ techniqueId: string }> }
) {
  const params = await props.params;
  try {
    const session = await getServerSession(authOptions);

    if (!session) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized' },
        { status: 401 }
      );
    }

    if (!mongoose.isValidObjectId(params.techniqueId)) {
      return NextResponse.json(
        { success: false, error: 'Invalid technique id' },
        { status: 400 }
      );
    }

    const body = await request.json();

    if (
      body.status !== undefined &&
      body.status !== null &&
      !VALID_STATUSES.includes(body.status)
    ) {
      return NextResponse.json(
        { success: false, error: 'Invalid status value' },
        { status: 400 }
      );
    }

    await dbConnect();

    const user = await User.findById(session.user.id);

    if (!user) {
      return NextResponse.json(
        { success: false, error: 'User not found' },
        { status: 404 }
      );
    }

    const existingIndex = user.mySkills.findIndex(
      (s) => s.technique.toString() === params.techniqueId
    );
    const existing = existingIndex >= 0 ? user.mySkills[existingIndex] : null;

    const nextStatus =
      body.status !== undefined ? body.status : existing?.status ?? null;
    const nextIsFavorite =
      body.isFavorite !== undefined ? body.isFavorite : existing?.isFavorite ?? false;

    if (nextStatus === 'signature') {
      const signatureCount = user.mySkills.filter(
        (s, i) => i !== existingIndex && s.status === 'signature'
      ).length;
      if (signatureCount >= 2) {
        return NextResponse.json(
          {
            success: false,
            error: '나를 대표하는 기술은 최대 2개까지 지정할 수 있습니다.',
          },
          { status: 400 }
        );
      }
    }

    if (nextStatus === null && !nextIsFavorite) {
      if (existingIndex >= 0) {
        user.mySkills.splice(existingIndex, 1);
      }
    } else if (existingIndex >= 0) {
      user.mySkills[existingIndex].status = nextStatus;
      user.mySkills[existingIndex].isFavorite = nextIsFavorite;
    } else {
      user.mySkills.push({
        technique: new mongoose.Types.ObjectId(params.techniqueId),
        status: nextStatus,
        isFavorite: nextIsFavorite,
      });
    }

    await user.save();

    return NextResponse.json({
      success: true,
      data: { status: nextStatus, isFavorite: nextIsFavorite },
    });
  } catch (error) {
    console.error('PUT /api/user/me/skills/[techniqueId] error:', error);
    return NextResponse.json(
      { success: false, error: 'Failed to update skill' },
      { status: 500 }
    );
  }
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 기술 하나 가져오기**

```bash
TECH_ID=$(curl -s "http://localhost:3000/api/techniques?status=published" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>console.log(JSON.parse(d).data[0]._id))")
echo "TECH_ID=$TECH_ID"
```

- [ ] **Step 4: 초기 상태(GET) 확인 — 아직 아무것도 없으면 null/false**

```bash
COOKIE_JAR=/tmp/task_skills_list.txt   # Task 2에서 로그인한 쿠키 재사용, 없으면 새로 로그인
curl -s -b "$COOKIE_JAR" "http://localhost:3000/api/user/me/skills/$TECH_ID"
```
Expected: `{"success":true,"data":{"status":null,"isFavorite":false}}` (이미 이 기술에 뭔가 설정해둔 적이 있다면 그 값이 나올 수 있음 — 그 경우는 다음 단계에서 원하는 값으로 다시 설정해서 확인).

- [ ] **Step 5: status를 `interested`로 설정**

```bash
curl -i -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$TECH_ID" \
  -H "Content-Type: application/json" \
  -d '{"status":"interested"}'
```
Expected: `200`, `{"success":true,"data":{"status":"interested","isFavorite":false}}`.

- [ ] **Step 6: 같은 요청을 GET으로 다시 확인**

```bash
curl -s -b "$COOKIE_JAR" "http://localhost:3000/api/user/me/skills/$TECH_ID"
```
Expected: `{"success":true,"data":{"status":"interested","isFavorite":false}}`.

- [ ] **Step 7: isFavorite만 별도로 true로 설정 (status는 유지되는지 확인)**

```bash
curl -s -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$TECH_ID" \
  -H "Content-Type: application/json" \
  -d '{"isFavorite":true}'
```
Expected: `{"success":true,"data":{"status":"interested","isFavorite":true}}` (status가 `interested`로 유지됨 — 부분 업데이트 확인).

- [ ] **Step 8: 잘못된 status 값 거부 확인**

```bash
curl -i -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$TECH_ID" \
  -H "Content-Type: application/json" \
  -d '{"status":"not-a-real-status"}'
```
Expected: `400`.

- [ ] **Step 9: signature 3개째 시도 시 400 확인**

서로 다른 기술 3개의 ID를 구해서 순서대로 signature로 설정:
```bash
IDS=$(curl -s "http://localhost:3000/api/techniques?status=published" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const arr=JSON.parse(d).data.slice(0,3).map(t=>t._id);console.log(arr.join(' '))})")
set -- $IDS
curl -s -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$1" -H "Content-Type: application/json" -d '{"status":"signature"}'
curl -s -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$2" -H "Content-Type: application/json" -d '{"status":"signature"}'
curl -i -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$3" -H "Content-Type: application/json" -d '{"status":"signature"}'
```
Expected: 첫 두 개는 `200`, 세 번째는 `400`과 `"나를 대표하는 기술은 최대 2개까지 지정할 수 있습니다."` 메시지.

- [ ] **Step 10: status와 isFavorite을 둘 다 해제하면 항목이 사라지는지 확인**

```bash
curl -s -b "$COOKIE_JAR" -X PUT "http://localhost:3000/api/user/me/skills/$TECH_ID" \
  -H "Content-Type: application/json" \
  -d '{"status":null,"isFavorite":false}'
curl -s -b "$COOKIE_JAR" http://localhost:3000/api/user/me/skills
```
Expected: PUT 응답은 `{"status":null,"isFavorite":false}`이고, 목록 조회 결과에는 이 기술 항목이 더 이상 없어야 한다(Step 9에서 설정한 signature 2개는 남아있어야 함).

- [ ] **Step 11: Commit**

```bash
git add "src/app/api/user/me/skills/[techniqueId]/route.ts"
git commit -m "$(cat <<'EOF'
Add GET/PUT /api/user/me/skills/[techniqueId] with signature cap

PUT does a partial upsert (status and/or isFavorite), enforces the
enum on status, caps status:'signature' at 2 per user, and removes
the entry entirely once both status and isFavorite are cleared.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 기술 상세 페이지 — 분류 컨트롤

**Files:**
- Create: `src/components/technique/SkillStatusControls.tsx`
- Modify: `src/app/technique/[...slug]/page.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useSession } from 'next-auth/react';
import { Heart } from 'lucide-react';

type SkillStatus = 'interested' | 'practicing' | 'frequently_used' | 'signature';

const STATUS_OPTIONS: { value: SkillStatus; label: string }[] = [
  { value: 'interested', label: '흥미생김' },
  { value: 'practicing', label: '연습중' },
  { value: 'frequently_used', label: '즐겨씀' },
  { value: 'signature', label: '나를 대표하는 기술' },
];

interface SkillStatusControlsProps {
  techniqueId: string;
}

export function SkillStatusControls({ techniqueId }: SkillStatusControlsProps) {
  const { status: sessionStatus } = useSession();
  const [skillStatus, setSkillStatus] = useState<SkillStatus | null>(null);
  const [isFavorite, setIsFavorite] = useState(false);
  const [loaded, setLoaded] = useState(false);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  useEffect(() => {
    async function fetchSkill() {
      if (sessionStatus !== 'authenticated') return;
      try {
        const res = await fetch(`/api/user/me/skills/${techniqueId}`);
        const data = await res.json();
        if (data.success) {
          setSkillStatus(data.data.status);
          setIsFavorite(data.data.isFavorite);
        }
      } finally {
        setLoaded(true);
      }
    }
    fetchSkill();
  }, [sessionStatus, techniqueId]);

  async function updateSkill(update: { status?: SkillStatus | null; isFavorite?: boolean }) {
    setSaving(true);
    setError('');
    try {
      const res = await fetch(`/api/user/me/skills/${techniqueId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(update),
      });
      const data = await res.json();
      if (!data.success) {
        setError(data.error || '저장하지 못했습니다.');
        return;
      }
      if (update.status !== undefined) setSkillStatus(update.status);
      if (update.isFavorite !== undefined) setIsFavorite(update.isFavorite);
    } catch {
      setError('오류가 발생했습니다.');
    } finally {
      setSaving(false);
    }
  }

  function handleStatusClick(value: SkillStatus) {
    const nextStatus = skillStatus === value ? null : value;
    updateSkill({ status: nextStatus });
  }

  function handleFavoriteClick() {
    updateSkill({ isFavorite: !isFavorite });
  }

  if (sessionStatus !== 'authenticated' || !loaded) {
    return null;
  }

  return (
    <div className="flex flex-wrap items-center gap-2 py-4 border-y border-border">
      {STATUS_OPTIONS.map((option) => (
        <button
          key={option.value}
          type="button"
          disabled={saving}
          onClick={() => handleStatusClick(option.value)}
          className={`inline-flex items-center rounded-full px-3 py-1.5 text-sm font-medium transition-colors disabled:opacity-50 ${
            skillStatus === option.value
              ? 'bg-primary text-primary-foreground'
              : 'bg-muted text-muted-foreground hover:bg-accent hover:text-accent-foreground'
          }`}
        >
          {option.label}
        </button>
      ))}
      <button
        type="button"
        disabled={saving}
        onClick={handleFavoriteClick}
        aria-label="좋아하는 기술로 표시"
        className={`inline-flex items-center gap-1 rounded-full px-3 py-1.5 text-sm font-medium transition-colors disabled:opacity-50 ${
          isFavorite
            ? 'bg-destructive/10 text-destructive'
            : 'bg-muted text-muted-foreground hover:bg-accent hover:text-accent-foreground'
        }`}
      >
        <Heart className={`h-4 w-4 ${isFavorite ? 'fill-current' : ''}`} />
        좋아함
      </button>
      {error && <p className="w-full text-sm text-destructive">{error}</p>}
    </div>
  );
}
```

- [ ] **Step 2: 기술 상세 페이지에 삽입**

`src/app/technique/[...slug]/page.tsx` 상단 import에 추가:
```tsx
import { SkillStatusControls } from '@/components/technique/SkillStatusControls';
```

`</header>` 바로 다음, `{/* Media */}` 주석 바로 앞에 추가:
```tsx
        {!isEditing && <SkillStatusControls techniqueId={technique._id} />}

```

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 4: 로그인 상태에서 실제로 동작하는지 curl로 흐름 확인**

Task 3에서 쓴 쿠키/기술 ID를 재사용해, 이 기술의 상세 페이지 SSR HTML에 컨트롤이 포함되는지는 클라이언트 컴포넌트라 curl로 직접 확인이 어렵다(로딩 상태만 보임). 대신:
1. `GET /api/user/me/skills/$TECH_ID`로 현재 값을 확인.
2. 컴포넌트 코드를 다시 읽고 `fetch`/`updateSkill` 호출이 위 API와 필드명이 일치하는지 확인.
3. `npx tsc --noEmit`이 깨끗한지 확인.

(실제 화면 동작은 컨트롤러가 브라우저로 최종 확인한다.)

- [ ] **Step 5: Commit**

```bash
git add src/components/technique/SkillStatusControls.tsx "src/app/technique/[...slug]/page.tsx"
git commit -m "$(cat <<'EOF'
Add skill status/favorite controls to the technique detail page

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 프로필 페이지 — "내 기술" 섹션

**Files:**
- Create: `src/components/profile/MySkillsSection.tsx`
- Modify: `src/app/profile/page.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { Heart, X } from 'lucide-react';

type SkillStatus = 'interested' | 'practicing' | 'frequently_used' | 'signature';

interface SkillEntry {
  technique: { _id: string; slug: string; name: { ko: string; en?: string }; pathSlugs: string[] };
  status: SkillStatus | null;
  isFavorite: boolean;
}

const STATUS_GROUPS: { value: SkillStatus; label: string }[] = [
  { value: 'interested', label: '흥미생김' },
  { value: 'practicing', label: '연습중' },
  { value: 'frequently_used', label: '즐겨씀' },
  { value: 'signature', label: '나를 대표하는 기술' },
];

function techniqueHref(technique: SkillEntry['technique']) {
  return `/technique/${[...(technique.pathSlugs || []), technique.slug].join('/')}`;
}

export function MySkillsSection() {
  const [skills, setSkills] = useState<SkillEntry[] | null>(null);
  const [error, setError] = useState('');

  useEffect(() => {
    async function fetchSkills() {
      try {
        const res = await fetch('/api/user/me/skills');
        const data = await res.json();
        if (data.success) {
          setSkills(data.data);
        } else {
          setError(data.error || '내 기술을 불러오지 못했습니다.');
        }
      } catch {
        setError('오류가 발생했습니다.');
      }
    }
    fetchSkills();
  }, []);

  async function updateSkill(techniqueId: string, update: { status?: SkillStatus | null; isFavorite?: boolean }) {
    try {
      const res = await fetch(`/api/user/me/skills/${techniqueId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(update),
      });
      const data = await res.json();
      if (!data.success) return;

      setSkills((prev) => {
        if (!prev) return prev;
        return prev
          .map((entry) =>
            entry.technique._id === techniqueId
              ? {
                  ...entry,
                  status: update.status !== undefined ? update.status : entry.status,
                  isFavorite: update.isFavorite !== undefined ? update.isFavorite : entry.isFavorite,
                }
              : entry
          )
          .filter((entry) => entry.status !== null || entry.isFavorite);
      });
    } catch {
      setError('오류가 발생했습니다.');
    }
  }

  if (error) {
    return (
      <div className="p-4 bg-destructive/10 text-destructive rounded-md mt-6">
        {error}
      </div>
    );
  }

  if (!skills) {
    return <div className="mt-6 text-muted-foreground">불러오는 중...</div>;
  }

  const favorites = skills.filter((s) => s.isFavorite);

  return (
    <div className="mt-10 space-y-8">
      <h2 className="text-2xl font-bold">내 기술</h2>

      {skills.length === 0 && (
        <p className="text-muted-foreground">
          아직 분류한 기술이 없습니다. 기술 상세 페이지에서 추가해보세요.
        </p>
      )}

      {STATUS_GROUPS.map((group) => {
        const items = skills.filter((s) => s.status === group.value);
        if (items.length === 0) return null;
        return (
          <div key={group.value}>
            <h3 className="text-lg font-semibold mb-3">{group.label}</h3>
            <ul className="space-y-2">
              {items.map((entry) => (
                <li
                  key={entry.technique._id}
                  className="flex items-center justify-between rounded-md border p-3"
                >
                  <Link href={techniqueHref(entry.technique)} className="hover:underline">
                    {entry.technique.name.ko}
                  </Link>
                  <button
                    onClick={() => updateSkill(entry.technique._id, { status: null })}
                    aria-label="분류 해제"
                    className="text-muted-foreground hover:text-destructive"
                  >
                    <X className="h-4 w-4" />
                  </button>
                </li>
              ))}
            </ul>
          </div>
        );
      })}

      {favorites.length > 0 && (
        <div>
          <h3 className="text-lg font-semibold mb-3">좋아하는 기술</h3>
          <ul className="space-y-2">
            {favorites.map((entry) => (
              <li
                key={entry.technique._id}
                className="flex items-center justify-between rounded-md border p-3"
              >
                <Link href={techniqueHref(entry.technique)} className="hover:underline">
                  {entry.technique.name.ko}
                </Link>
                <button
                  onClick={() => updateSkill(entry.technique._id, { isFavorite: false })}
                  aria-label="좋아요 해제"
                  className="text-destructive hover:text-muted-foreground"
                >
                  <Heart className="h-4 w-4 fill-current" />
                </button>
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 프로필 페이지에 삽입**

`src/app/profile/page.tsx` 상단 import에 추가:
```tsx
import { MySkillsSection } from '@/components/profile/MySkillsSection';
```

기존:
```tsx
      {profile && (
        <div className="space-y-4 rounded-lg border p-6">
          ...
        </div>
      )}
    </div>
  );
}
```
다음으로 교체(내부 카드 내용은 그대로 두고, 그 블록 뒤에 새 섹션만 추가):
```tsx
      {profile && (
        <div className="space-y-4 rounded-lg border p-6">
          ...
        </div>
      )}

      {profile && <MySkillsSection />}
    </div>
  );
}
```

(`MySkillsSection`은 `profile`이 로드된 뒤에만, 즉 로그인이 확정된 뒤에만 렌더링되게 한다.)

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 4: curl로 데이터가 올바르게 오는지 최종 확인**

```bash
curl -s -b "$COOKIE_JAR" http://localhost:3000/api/user/me/skills
```
Task 3에서 설정한 항목(예: signature 2개)이 populate된 `technique.name`/`slug`/`pathSlugs`와 함께 나오는지 확인. 이 응답 구조가 `MySkillsSection`이 기대하는 `SkillEntry[]`와 일치하는지 코드로 재확인한다.

- [ ] **Step 5: Commit**

```bash
git add src/components/profile/MySkillsSection.tsx src/app/profile/page.tsx
git commit -m "$(cat <<'EOF'
Add My Skills section to the profile page

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```
